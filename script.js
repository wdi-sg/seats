console.log("hello script js");

//assign ticket price and number of seats as variables
let ticketPrice, numOfSeats;

ticketPrice = 50 ;
numOfSeats = 10 ;


var inputHappened = function(currentInput){
  console.log( currentInput );


// ticket price for first seat
if ( numOfSeats === 10 ) {
    // calculate number of seats left
while ( numOfSeats <= 0);
//console.log(numOfSeats);
numOfSeats = numOfSeats - 1;
//console.log(numOfSeats);
    return ticketPrice;
    //ticket price for the balance seats
} else {
    return (ticketPrice * (5/100) + ticketPrice);
}



}